{
  "name": "smart-bi",
  "version": "1.0.0",
  "private": true,
  "description": "Smart BI 数据仪表盘 - 大模型工程能力测试靶场",
  "workspaces": [
    "frontend",
    "mock-server"
  ],
  "scripts": {
    "dev": "npm run dev --workspaces --if-present",
    "build": "npm run build --workspaces --if-present",
    "start": "npm run start --workspace mock-server",
    "test": "npm run test --workspaces --if-present",
    "lint": "echo \"lint not configured\" && exit 0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}